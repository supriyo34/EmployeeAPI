apiVersion: v1
kind: Service
metadata:
  name: {{ include "employeeapi-chart.fullname" . }}
  labels:
    {{- include "employeeapi-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "employeeapi-chart.selectorLabels" . | nindent 4 }}
