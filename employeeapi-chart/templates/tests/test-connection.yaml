apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "employeeapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "employeeapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "employeeapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
